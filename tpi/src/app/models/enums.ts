export enum DocumentType {
    DNI = 'DNI',
    PASSPORT = 'PASSPORT',
    CUIL = 'CUIL',
}

export enum EmployeeType {
    PERMANENT = 'PERMANENT',
    CONTRACTOR = 'CONTRACTOR',
    INTERN = 'INTERN',
}